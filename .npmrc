# Registry configuration
registry=https://registry.npmjs.org/

# Publishing configuration
access=public

# Save exact versions (optional, recommended for monorepo)
save-exact=true

# Don't create package-lock.json (we use bun.lock)
package-lock=false

