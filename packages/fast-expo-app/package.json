{
  "name": "fast-expo-app",
  "version": "3.1.0",
  "description": "⚡ Fast & modern CLI to create production-ready Expo apps with TypeScript, NativeWind v4, Unistyles v3, MMKV, and more",
  "main": "dist/index.js",
  "bin": {
    "fast-expo-app": "./bin/fast-expo-app.js"
  },
  "scripts": {
    "build": "tsc --project ../../cli/tsconfig.json && bun run copy-template && bun run copy-modules && bun run clean-template",
    "copy-template": "rm -rf templates && cp -r ../../cli/templates templates",
    "copy-modules": "rm -rf modules && cp -r ../../cli/modules modules",
    "clean-template": "find templates -type d -name 'node_modules' -prune -exec rm -rf {} + && find templates -type d -name '.expo' -prune -exec rm -rf {} + && find templates -type d -name 'Pods' -prune -exec rm -rf {} + && find templates -type d -name 'build' -prune -exec rm -rf {} + && rm -f templates/**/bun.lock templates/**/package-lock.json templates/**/yarn.lock",
    "dev": "tsc --project ../../cli/tsconfig.json --watch",
    "prepublishOnly": "bun run build",
    "publish:patch": "bun run publish.ts patch",
    "publish:minor": "bun run publish.ts minor",
    "publish:major": "bun run publish.ts major"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Teczer/expo-react-native-nativewind-typescript-boilerplate.git",
    "directory": "packages/fast-expo-app"
  },
  "keywords": [
    "expo",
    "react-native",
    "nativewind",
    "unistyles",
    "typescript",
    "cli",
    "boilerplate",
    "mmkv",
    "react-query",
    "zustand",
    "expo-router",
    "mobile-app",
    "starter-template"
  ],
  "author": "Teczer",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Teczer/expo-react-native-nativewind-typescript-boilerplate/issues"
  },
  "homepage": "https://github.com/Teczer/expo-react-native-nativewind-typescript-boilerplate#readme",
  "files": [
    "dist",
    "bin",
    "templates",
    "modules",
    "README.md"
  ],
  "type": "module",
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "kleur": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
